# importing numpy
import numpy as np
# to create array
a=np.array([1,2,3])
# to find dimension
print(a.ndim)
# to find order
print(a.shape)
# to find total elements
print(a.size)
# to check datatype of matrix
print(a.dtype)